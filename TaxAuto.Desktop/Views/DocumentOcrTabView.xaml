<UserControl x:Class="TaxAuto.Desktop.Views.DocumentOcrTabView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d">

    <Grid Margin="12">

        <Grid.RowDefinitions>

            <!-- 버튼 -->
            <RowDefinition Height="Auto"/>

            <!-- 파일 목록 -->
            <RowDefinition Height="*"
                           MinHeight="150"/>

            <!-- Splitter -->
            <RowDefinition Height="5"/>

            <!-- 로그 -->
            <RowDefinition Height="180"
                           MinHeight="100"/>

        </Grid.RowDefinitions>


        <!-- 버튼 -->
        <StackPanel Orientation="Horizontal"
                    Margin="0,0,0,8">

            <Button Content="파일 선택"
                    Width="100"
                    Height="32"
                    Margin="0,0,8,0"
                    Command="{Binding SelectFilesCommand}" />

            <Button Content="OCR 실행"
                    Width="100"
                    Height="32"
                    Margin="0,0,8,0"
                    Command="{Binding RunOcrCommand}" />

            <Button Content="Clear"
                    Width="100"
                    Height="32"
                    Margin="0,0,8,0"
                    Command="{Binding ClearCommand}" />

            <Button Content="엑셀 내보내기"
                    Width="100"
                    Height="32"
                    Command="{Binding ExportExcelCommand}" />

        </StackPanel>


        <!-- 파일 목록 -->
        <DataGrid Grid.Row="1"
                  ItemsSource="{Binding Jobs}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  IsReadOnly="True">

            <DataGrid.Columns>

                <DataGridTextColumn Header="파일명"
                                    Binding="{Binding FileName}"
                                    Width="2*" />

                <DataGridTextColumn Header="상태"
                                    Binding="{Binding Status}"
                                    Width="*" />

                <DataGridTextColumn Header="실행시간"
                                    Binding="{Binding ElapsedText}"
                                    Width="*" />

                <DataGridTextColumn Header="결과 JSON"
                                    Binding="{Binding ResultJsonPath}"
                                    Width="3*" />

                <DataGridTextColumn Header="메시지"
                                    Binding="{Binding Message}"
                                    Width="2*" />

            </DataGrid.Columns>

        </DataGrid>


        <!-- 높이 조절 바 -->
        <GridSplitter Grid.Row="2"
                      Height="5"
                      HorizontalAlignment="Stretch"
                      VerticalAlignment="Center"
                      ResizeDirection="Rows"
                      ResizeBehavior="PreviousAndNext"
                      Background="LightGray"
                      ShowsPreview="True"/>


        <!-- 로그 -->
        <TextBox Grid.Row="3"
                 Margin="0,8,0,0"
                 Text="{Binding LogText}"
                 IsReadOnly="True"
                 TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto"
                 AcceptsReturn="True" />

    </Grid>

</UserControl>